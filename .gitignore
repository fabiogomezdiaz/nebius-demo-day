# Official Soperator recipe is fetched by terraform init (git::). A leftover
# local clone is unused and not committed.
vendor/nebius-solutions-library/

# Terraform
.terraform/
.terraform.lock.hcl
.terraform.tfstate.lock.info
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
crash.log
crash.*.log
tfplan
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Local tfvars with secrets. The committed overlay is terraform.tfvars (placeholder SSH key only).
*.tfvars.local
*.auto.tfvars
*.auto.tfvars.json

# Secrets and local env (filled after sandbox access)
.env
.envrc
.env.local

# Local kubeconfig written by terraform apply (platform/workloads providers).
terraform/kubeconfig
**/kubeconfig
!**/kubeconfig.example
terraform/**/login.sh
terraform/**/assets/render/
terraform/soperator-recipe
*.pem
id_rsa
id_ed25519
*credentials*
hf_token
.huggingface/

# Python
.venv/
.venv-pptx/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Workload outputs
workloads/outputs/
workloads/checkpoints/
workloads/hf_cache/
workloads/models/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
presentation/~$*
