# Official Soperator recipe is fetched by terraform init (git::). A leftover
# local clone is unused and not committed.
vendor/nebius-solutions-library/

# Terraform
.terraform/
.terraform.lock.hcl
.terraform.tfstate.lock.info
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
crash.log
crash.*.log
tfplan
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Local tfvars with secrets. The committed overlay is terraform.tfvars (SSH key path, not the key).
*.tfvars.local
*.auto.tfvars
*.auto.tfvars.json

# Secrets and local env (filled after sandbox access)
.env
.envrc
.env.local

# Local kubeconfig written by terraform apply (platform providers).
terraform/kubeconfig
**/kubeconfig
!**/kubeconfig.example
terraform/**/assets/render/
terraform/soperator-recipe
*.pem
id_rsa
id_ed25519
*credentials*
hf_token
.huggingface/

# Python
.venv/
.venv-pptx/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Compare output from ./task-3/03-compare.sh
task-3/out/

# Local training caches (cluster I/O lives on /mnt/data)
task-1/outputs/
task-1/checkpoints/
task-1/hf_cache/
task-1/models/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
presentation/~$*
